# Vite recommends using .env.local or .env.[mode].local for secrets.
# SEE: https://vite.dev/guide/env-and-mode.html#env-files

# Default NODE_ENV with `vite build --mode=test` is production
NODE_ENV=development
